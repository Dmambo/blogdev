<%= form_with model: Comment.new, local: true, url: user_post_comments_path(@post.author, @post),
html: {class: "column form"} do |f| %>
  <%= f.text_area :text, placeholder: "Comment ...", required: true %>
  <%= f.submit "Add Comment" %>
<% end %>